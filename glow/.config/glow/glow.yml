# style name or JSON path (default "auto")
style: "auto"
#style: "light"

# mouse support (TUI-mode only)
mouse: true

# use pager/tui to display markdown
#pager: true
tui: true

# word-wrap at width
width: 80

# show all files, including hidden and ignored.
all: false

# show line numbers (TUI-mode only)
showLineNumbers: true

